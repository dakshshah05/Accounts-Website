// Firebase logic disconnected for pure local mockup to prevent Vercel crashes.
// Replace with actual firebase init logic when you have config values.

export const auth = {};
export const db = {};
export const storage = {};
